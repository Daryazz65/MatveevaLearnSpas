<Page x:Class="MatveevaLearnSpas.View.Pages.JournalPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MatveevaLearnSpas.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1620"
      Title="JournalPage">
    <Grid>
        <StackPanel>
            <TextBlock Text="Название лекции:" Foreground="White"/>
            <TextBox x:Name="LectureNameTb" Margin="10" Foreground="White"/>
            <TextBlock Text="Вопросы и ответы:" Margin="10" Foreground="White"/>
            <DataGrid x:Name="QuestionsDG" AutoGenerateColumns="False" CanUserAddRows="True" Height="815" Foreground="White" Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Вопрос" Binding="{Binding Question}" Width="2*"/>
                    <DataGridTextColumn Header="Правильный ответ" Binding="{Binding CorrectAnswer}" Width="*"/>
                    <DataGridTextColumn Header="Неверный ответ 1" Binding="{Binding IncorrectAnswer1}" Width="*"/>
                    <DataGridTextColumn Header="Неверный ответ 2" Binding="{Binding IncorrectAnswer2}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Добавить лекцию" Click="AddLectureBtn_Click" Margin="10"/>
        </StackPanel>
    </Grid>
</Page>
